---
name: workflow-trigger-node
description: Index of workflow trigger templates. Read this guide before selecting a trigger leaf.
---

# Workflow Trigger Templates

Available trigger leaves:
- `skills/workflow/trigger_node/timeoff_created_event/SKILL.md` — trigger on a newly created time-off request.

The product also supports additional backend-defined event and date-based triggers. Use the direct-answer workflow leaves for guidance, but do not construct an executable trigger payload unless a matching trigger leaf defines its exact contract.

## Usage
- Start here to discover supported workflow trigger events.
- Read the selected trigger leaf before building conditions or event payload references.
- Do not execute this index node.

## Child entrypoints
- `skills/workflow/trigger_node/timeoff_created_event/SKILL.md`
